// No domain tables yet — seeding lands with the schema port.
// ponytail: placeholder so `pnpm --filter @app/db seed` resolves; fill in after the schema port.
console.log("Nothing to seed yet (no domain tables).");
